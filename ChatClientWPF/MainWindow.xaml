<Window x:Class="ChatClientWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChatClientWPF"
        mc:Ignorable="d"
        Title="Chat#" Height="640" Width="360" MinWidth="360" MinHeight="400">
    <Grid>
        <TabControl x:Name="tabControl" BorderThickness="0,1,0,0">
            <TabItem x:Name="tab_Connect" Header="Connect" Width="80" Height="24" Margin="-2">
                <Grid>
                    <StackPanel Width="240" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,80,0,0">
                        <Label Content="1. Enter the address of the server:" Foreground="#CC000000"/>
                        <TextBox x:Name="tbxServerAddress" Height="23" Text="127.0.0.1" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                        <Label Content="2. Enter a username you want to use:" Margin="0,16,0,0" Foreground="#CC000000"/>
                        <TextBox x:Name="tbxUsername" Height="23" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Text="MyName"/>
                        <Label Content="3. Click to connect to the server:" Margin="0,16,0,0" Foreground="#CC000000"/>
                        <Button x:Name="btnConnect" Content="Connect" Height="32" Click="btnConnect_Click"/>
                        <Label x:Name="lblConnectionStatus1" Content="Connection Successful!" 
                               Margin="0,48,0,48" HorizontalAlignment="Center" Foreground="#FF2FA856"/>
                        <Label x:Name="lblConnectionStatus2" Content="Connection Failed...  __(:3 」∠)_" 
                               Margin="0,48,0,48" HorizontalAlignment="Center" Foreground="#FFEE3939"/>
                        <Button x:Name="btnDisconnect" Content="Disconnect" Height="32" Click="btnDisconnect_Click"/>
                    </StackPanel>
                </Grid>
            </TabItem>
            <TabItem x:Name="tab_Users" Header="Online Users" Width="120" Height="24" Margin="-4,-2,-2,-2">
                <Grid>
                    <ListBox x:Name="listBox_Users" Margin="8,8,8,80">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Background="Transparent" MouseDown="listBox_Users_ItemMouseDown">
                                    <Label Content="{Binding Path=DisplayName}" FontWeight="Bold"/>
                                    <Label Content="{Binding Path=ClientId}" Foreground="#50000000" Margin="0,-4,0,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <Button x:Name="TESTBUTTON" Content="Button" Height="65" Margin="10,0,10,10" VerticalAlignment="Bottom"/>
                </Grid>
            </TabItem>
            <TabItem x:Name="tab_Chatrooms" Header="Chatrooms" Width="120" Height="24" Margin="-6,-2,-2,-2">
                <Grid>
                    <ListBox x:Name="listBox_Chatrooms" Margin="8,8,8,80">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Background="Transparent" MouseDown="listBox_Chatrooms_ItemMouseDown">
                                    <Label Content="{Binding Path=ChatroomName}" FontWeight="Bold"/>
                                    <Label Content="{Binding Path=ChatroomId}" Foreground="#50000000" Margin="0,-4,0,0"/>
                                    <Label Content="{Binding Path=MemberCount}" Foreground="#50000000" Margin="0,-4,0,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </TabItem>
        </TabControl>

    </Grid>
</Window>
